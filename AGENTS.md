# Repository Instructions

Invoke `$repo-architecture-guard` before modifying this Skill.

Read `ProjectGoal.md`, `docs/ai/project-status.md`, and the code map before
implementation. Use TDD for runtime behavior. Preserve standard-library-only
Python compatibility and support Windows, macOS, and Linux.

Do not overwrite user-owned `AGENTS.md` content. Project initialization may
modify only the block delimited by:

```text
<!-- repo-architecture-guard:start -->
<!-- repo-architecture-guard:end -->
```

Run the full test suite, Skill validation, code-map update, and documentation
checks before publishing.
