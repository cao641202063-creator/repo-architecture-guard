interface:
  display_name: "Repository Architecture Guard"
  short_description: "Route delivery by impact, risk, and existing architecture."
  default_prompt: "Use $repo-architecture-guard to assess impact and risk, then select the minimum sufficient workflow and verification."
